import Link from 'next/link';
import { cefrLevels, resourceCategories, type ResourceCategory } from '@/data';
import type { ResourceFilters, TextbookFilters } from '@/lib/catalog-filters';
import { resourceCategoryLabels } from '@/lib/catalog-labels';

const fieldClass = 'mt-1 min-h-11 w-full rounded-md border bg-background px-3 text-sm focus-visible:ring-2 focus-visible:ring-ring';
export function TextbookFilterForm({ filters, publishers, formats, purposes }: { filters: TextbookFilters; publishers: string[]; formats: string[]; purposes: string[] }) { return <form className="mt-6 grid gap-3 rounded-xl border bg-muted/30 p-4 sm:mt-8 sm:grid-cols-2 lg:grid-cols-4"><label className="text-sm font-medium">Уровень<select name="level" defaultValue={filters.level ?? ''} className={fieldClass}><option value="">Все уровни</option>{cefrLevels.map((level) => <option key={level}>{level}</option>)}</select></label><label className="text-sm font-medium">Издатель<select name="publisher" defaultValue={filters.publisher ?? ''} className={fieldClass}><option value="">Все издатели</option>{publishers.map((item) => <option key={item}>{item}</option>)}</select></label><label className="text-sm font-medium">Формат<select name="format" defaultValue={filters.format ?? ''} className={fieldClass}><option value="">Все форматы</option>{formats.map((item) => <option key={item}>{item}</option>)}</select></label><label className="text-sm font-medium">Назначение<select name="purpose" defaultValue={filters.purpose ?? ''} className={fieldClass}><option value="">Любое назначение</option>{purposes.map((item) => <option key={item}>{item}</option>)}</select></label><div className="grid grid-cols-[minmax(0,1fr)_auto] items-center gap-3 sm:col-span-2 sm:flex sm:items-end lg:col-span-4"><button className="min-h-11 w-full rounded-md bg-primary px-4 text-sm font-semibold text-primary-foreground hover:bg-primary/90 focus-visible:ring-2 sm:w-auto">Применить</button><Link href="/textbooks" className="inline-flex min-h-11 items-center justify-center px-2 text-sm font-semibold text-primary hover:underline sm:justify-start">Сбросить</Link></div></form>; }
export function ResourceFilterForm({ filters }: { filters: ResourceFilters }) { return <form className="mt-6 grid gap-4 rounded-xl border bg-muted/30 p-4 sm:mt-8 sm:grid-cols-[minmax(0,1fr)_auto_auto]"><label className="text-sm font-medium">Категория<select name="category" defaultValue={filters.category ?? ''} className={fieldClass}><option value="">Все категории</option>{resourceCategories.map((item) => <option value={item} key={item}>{resourceCategoryLabels[item]}</option>)}</select></label><label className="flex min-h-11 cursor-pointer items-center gap-3 rounded-md border bg-background px-3 text-sm font-medium sm:items-end sm:border-0 sm:bg-transparent sm:px-0 sm:pb-3"><input type="checkbox" name="free" value="true" defaultChecked={filters.free} className="h-5 w-5 shrink-0 sm:h-4 sm:w-4" />Только бесплатные</label><div className="grid grid-cols-[minmax(0,1fr)_auto] items-center gap-3 sm:flex sm:items-end"><button className="min-h-11 w-full rounded-md bg-primary px-4 text-sm font-semibold text-primary-foreground hover:bg-primary/90 focus-visible:ring-2 sm:w-auto">Показать</button><Link href="/resources" className="inline-flex min-h-11 items-center justify-center px-2 text-sm font-semibold text-primary hover:underline sm:justify-start">Сбросить</Link></div></form>; }
